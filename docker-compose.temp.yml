version: "3.3"
services:
  aimelia:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /aimelia
    ports:
      - 9000:8000
    volumes:
      - ./src:/src
    networks:
      - default
